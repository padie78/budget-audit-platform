// Este archivo se REGENERA en CI con los outputs reales de Terraform.
// Mantén las claves vacías en main para evitar filtrar credenciales.
export const environment = {
  production: true,
  appsync: {
    endpoint: '',
    region: '',
    apiKey: '',
  },
};
